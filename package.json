{
  "name": "polkadot-task-01",
  "description": "",
  "homepage": ".",
  "private": true,
  "author": "",
  "scripts": {
    "start": "node app-server.js",
    "build": "yarn build:front",
    "build:front": "cd client && yarn install && npx yarn build",
    "acp": "func() { git add --all && git commit -m ${1} && git push origin ${2} ;}; func"
  },
  "dependencies": {
    "cookie-parser": "^1.4.5",
    "express": "^4.17.1",
    "express-basic-auth": "^1.2.0"
  }
}
