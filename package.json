{
  "name": "polkadot-task-01",
  "description": "",
  "homepage": ".",
  "private": true,
  "author": "",
  "devDependencies": {
  },
  "scripts": {
    "start": "node app-server.js",
    "build:front": "cd client && npx yarn build",
    "acp": "func() { git add --all && git commit -m ${1} && git push origin ${2} ;}; func"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
